const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.main-nav a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(item=>observer.observe(item));document.getElementById('year').textContent=new Date().getFullYear();
const header=document.querySelector('.site-header');window.addEventListener('scroll',()=>header?.classList.toggle('scrolled',window.scrollY>30));
const reduceMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const bubbleField=document.querySelector('.bubble-field');if(bubbleField&&!reduceMotion){const count=window.innerWidth<700?10:20;for(let i=0;i<count;i++){const bubble=document.createElement('span');bubble.className='float-bubble';const size=Math.round(14+Math.random()*54);Object.assign(bubble.style,{width:`${size}px`,height:`${size}px`,left:`${Math.random()*100}%`,animationDuration:`${9+Math.random()*13}s`,animationDelay:`${-Math.random()*18}s`});bubbleField.appendChild(bubble)}}
const messages=['Try the Dr. Fluff! 🥤','Need drinks for a party? 🎉','Don’t forget the cold foam! ☁️','Book us after our opening! 🎈','See you at our pop-up! 👋'];const message=document.getElementById('mascot-message');let messageIndex=0;if(message&&!reduceMotion)setInterval(()=>{messageIndex=(messageIndex+1)%messages.length;message.animate([{opacity:0,transform:'translateY(8px)'},{opacity:1,transform:'translateY(0)'}],{duration:350});message.textContent=messages[messageIndex]},3300);
const sparkleField=document.querySelector('.sparkle-field');if(sparkleField&&!reduceMotion){setInterval(()=>{const sparkle=document.createElement('span');sparkle.className='mascot-sparkle';sparkle.textContent=['✨','⭐','✦'][Math.floor(Math.random()*3)];sparkle.style.left=`${5+Math.random()*90}%`;sparkle.style.top=`${5+Math.random()*80}%`;sparkleField.appendChild(sparkle);sparkle.addEventListener('animationend',()=>sparkle.remove())},900)}
const mascotButton=document.getElementById('mascot-button');const confettiLayer=document.querySelector('.confetti-layer');let clicks=0;mascotButton?.addEventListener('click',()=>{clicks++;mascotButton.animate([{transform:'rotate(-3deg) scale(1)'},{transform:'rotate(5deg) scale(1.04)'},{transform:'rotate(0) scale(1)'}],{duration:420});if(clicks===10){clicks=0;if(message)message.textContent="You found Fluff's secret! 🥤🎉";for(let i=0;i<44;i++){const piece=document.createElement('span');piece.className='confetti';piece.textContent=['✨','🟧','⭐','🥤','☁️'][Math.floor(Math.random()*5)];piece.style.setProperty('--x',`${(Math.random()-.5)*900}px`);piece.style.setProperty('--y',`${(Math.random()-.5)*700}px`);confettiLayer.appendChild(piece);piece.addEventListener('animationend',()=>piece.remove())}}});
